import { Paper, Text } from '@mantine/core';

export function CareerChart() {
  return (
    <Paper withBorder shadow='sm' p='sm' radius='md'>
      <Text size='xl' fw={500}>
        Thống kê học sinh theo ngành
      </Text>
    </Paper>
  );
}
